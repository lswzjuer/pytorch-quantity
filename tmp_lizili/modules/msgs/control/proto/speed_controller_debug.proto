syntax = "proto2";

package roadstar.control;

// speed controller debug information
message SpeedControllerDebug {
  // desired velocity
  optional double desired_velocity = 1;
  // current velocity
  optional double current_velocity = 2;
  optional double deceleration_input = 3;
}
