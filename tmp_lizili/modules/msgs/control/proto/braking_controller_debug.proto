syntax = "proto2";

package roadstar.control;

// braking controller debug information
message BrakingControllerDebug {

  // vehicle longitudinal position error
  optional double e_pose = 1;
  // desired velocity
  optional double desired_velocity = 2;
  // vehicle current velocity 
  optional double current_velocity = 3;
}
