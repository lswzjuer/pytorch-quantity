syntax = "proto2";

package roadstar.perception;

message PerceptionConfig {
  enum PerceptionType {
    VELODYNE = 0;
    CAMERA = 1;
    VLP16 = 2;
    RADAR = 3;
    VELODYNE_MERGE = 4;
  };

repeated PerceptionType perception_types = 1 ;
}
